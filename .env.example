# Server-side settings only. Never use NEXT_PUBLIC_ or VITE_ for secrets.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol
DATA_DIR=.data
APP_HOST=127.0.0.1
PORT=8000
# Required when APP_HOST is not a loopback address.
WORKSPACE_PASSWORD=
# Set true behind an HTTPS reverse proxy.
COOKIE_SECURE=false
